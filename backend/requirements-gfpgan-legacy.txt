# Run this optional model adapter in a separate Python 3.10 environment.
# The upstream GFPGAN dependency graph is not compatible with this project's
# current Python 3.12 runtime.
gfpgan==1.3.8
basicsr==1.4.2
facexlib==0.3.0
opencv-python==4.9.0.80
